var app = require('angular').module('linkCollector');

app.directive('showList', require('./showList'));
app.directive('filter', require('./filter'));
app.directive('credentials', require('./credentials'));
app.directive('howto', require('./howto'));
